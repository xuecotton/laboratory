var app = new Vue({
    el: "#app",
    data: {
        activeName: 'first',
    },
    methods: {
        handleClick(tab, event) {
        },
        close_d(){
            window.location.href='../index.html'
        },
        close_z(){
            window.location.href='../index.html'
        }
    }
});